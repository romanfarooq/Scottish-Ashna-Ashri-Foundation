export const getAdminData = (req, res) => {
  res.json({ message: "Admin GET request successful" });
};

export const postAdminData = (req, res) => {
  res.json({ message: "Admin POST request successful" });
};
